import  pytest

@pytest.mark.parametrize('p, x, y', [
    (True,  1, 3),
    (False, 2, 3),
    (False, 1, 2),
])
def test_2_within(p, x, y):
    ''' Confirming that ``(x < y < z)`` in Python is a valid way to test
        that y is within range [x..z].
    '''
    assert p is (x < 2 < y)
